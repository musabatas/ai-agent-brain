// Mega menu removed — AI Dev Brain uses sidebar navigation only.
// This empty div preserves the header's justify-between flex layout.
export function MegaMenu() {
  return <div className="flex-1" />;
}
