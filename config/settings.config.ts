import { Settings } from './types';

export const APP_SETTINGS: Settings = {
  layout: '',
  container: 'fixed',
  layouts: {
    main: {
      sidebarCollapse: false,
      sidebarTheme: 'light',
    },
  },
};
